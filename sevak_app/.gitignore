# ── Dart & Flutter ──────────────────────────────────────────────────────────
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
.pub-cache/
.pub/

# ── Environment & Secrets (NEVER COMMIT) ────────────────────────────────────
.env
.env.*
*.env
google-services.json          # Add to Firebase CI secrets instead
GoogleService-Info.plist      # iOS Firebase config (not used in MVP)
lib/firebase_options.dart     # Generated by flutterfire — OK to commit if no secrets

# ── IDE ──────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.iml
*.ipr
*.iws
.DS_Store

# ── Android ──────────────────────────────────────────────────────────────────
android/local.properties
android/.gradle/
android/key.properties       # Signing keystore details
*.jks                         # Signing keystore file
*.keystore

# ── iOS (not in scope for MVP) ───────────────────────────────────────────────
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log

# ── Test artifacts ───────────────────────────────────────────────────────────
coverage/
